pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
            bat 'set t = \n'
            bat 'echo %tfvars% >> mytfvars123.tfvars'
            bat 'echo suc'
         }
      }
   }
}
