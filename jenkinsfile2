pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
            bat 'powershell'
            bat 'echo %tfvars% >> mytfvars123.tfvars'
            bat 'echo suc'
         }
      }
   }
}
