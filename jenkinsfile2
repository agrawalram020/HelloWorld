pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
            bat 'echo start'
            bat 'echo %tfvars% >> %workbench_name%.tfvars'
            bat 'python demo.py %workbench_name%.tfvars'
            bat 'echo final done'
         }
      }
   }
}
