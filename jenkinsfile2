pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
            bat 'set t=^&echo.'
            bat 'echo started >>start.txt'
            bat 'echo %tfvars% >> mytfvars123.tfvars'
         }
      }
   }
}
