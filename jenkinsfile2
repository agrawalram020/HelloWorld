pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
            bat 'echo first>>layers/first.txt'
            bat 'echo second>>%workbench_name%.txt'
         }
      }
   }
}
