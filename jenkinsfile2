pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
            bat 'echo starteded >> start.txt'
            bat '@echo %tfvars% >> mytfvars123.tfvars'
         }
      }
   }
}
