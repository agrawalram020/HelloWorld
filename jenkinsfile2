pipeline {
    agent any
    
    stages {
        stage('Write to file') { 
            steps {
                bat "echo %workbench_name%"
                bat "echo %tfvars% >> var.tfvars"
            }
        }

        stage('Terraform Init') { 
            steps {
                bat 'terraform init'
            }
        }

        stage('Terraform Plan') { 
            steps {
                bat 'terraform plan -var-file="var.tfvars"'
            }
        }
        stage('Terraform Apply') { 
            steps {
                bat 'echo "Apply"'
                bat 'terraform apply -auto-approve -var-file="var.tfvars"'
            }
        }

	}

}
