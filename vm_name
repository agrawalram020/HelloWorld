pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
            bat 'echo %tfvars%>>var1.txt'
            bat 'echo line2'
            bat 'echo %workbench_name%
         }
      }
   }
}
