pipeline {
    agent any
    
    stages {
        stage('Ok') {
            steps {
                echo "Ok"
            }
        }
                stage('send mail') {
            steps {
                emailext body: 'A Test EMail', recipientProviders: [[$class: 'DevelopersRecipientProvider'], [$class: 'RequesterRecipientProvider']], subject: "from step ${var1}"
                                script {
                    httpRequest customHeaders: [name: 'FOO'], url: 'http://127.0.0.1:8000/updatetocreated'
                    echo 'call url success'
                }
            }
        }
    }
}
