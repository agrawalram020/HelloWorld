pipeline {
    agent any
    
    stages {
        stage('Ok') {
            steps {
                echo "Ok"
                httpRequest "http://127.0.0.1:8000/demourlcheck/response-headers?param1=${var}"
            }
        }
                stage('send mail') {
            steps {
                    emailext body: 'jfldsfjdlsfjdslfjdslf', subject: 'from extended email demo', to: 'agrawalram020@gmail.com'

            }
        }
    }
}
