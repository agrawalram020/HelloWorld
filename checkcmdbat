pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
            bat 'echo ram'
            dir('dev/${var1}'){
            bat 'echo %var1%'
            }
            bat 'echo now'
         }
      }
      
     }
   }
